BigWorld.Map = CLASS({
	
	preset : () => {
		return SkyEngine.Node;
	},
	
	init : (inner, self, params) => {
		//REQUIRED: params
		//REQUIRED: params.mapData
		
		let mapData = params.mapData;
	}
});